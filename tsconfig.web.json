{
  "extends": "./tsconfig.json",
  "include": ["src/renderer/**/*", "typings/**/*", "package.json"], // 需要包含的文件或文件夹
  "compilerOptions": {
    "composite": true,
    "lib": ["ESNext", "DOM", "DOM.Iterable"], // 引入的内置库
    "types": ["element-plus/global"] // 需要引入的类型声明
  },
  "references": []
}

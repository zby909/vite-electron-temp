{
  "extends": "./tsconfig.json",
  "include": ["src/renderer/**/*", "typings/**/*", "package.json"], 
  "compilerOptions": {
    "composite": true,
    "lib": ["ESNext", "DOM", "DOM.Iterable"], // 引入的内置库
    "types": ["element-plus/global"] // 需要引入的类型声明
  },
}
